<div class="container mt-5">
  
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1>📋 Planos</h1>
    <div class="mb-3">
      <a href="/planos/new" class="btn btn-success">➕ Adicionar</a>
      <a href="/planos/export_pdf?format=pdf" class="btn btn-warning">📄 Exportar PDF</a>
    </div>
  </div>

  <div class="table-responsive">
    <table class="table table-striped">
      <thead class="table-dark">
        <tr>
          <th>ID</th>
          <th>Nome</th>
          <th>Valor</th>
          <th>Duração</th>
          <th>Ações</th>
        </tr>
      </thead>
      <tbody>
        <% @planos.each do |plano| %>
          <tr>
            <td><%= plano.id %></td>
            <td><%= plano.nome_plano %></td>
            <td>R$ <%= number_with_precision(plano.valor_mensal, precision: 2) %></td>
            <td><%= plano.duracao_meses %> meses</td>
            <td>
              <a href="/planos/<%= plano.id %>/edit" class="btn btn-sm btn-warning">✏️</a>
              <form action="/planos/<%= plano.id %>" method="POST" style="display:inline;">
                <input type="hidden" name="_method" value="DELETE">
                <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
                <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Tem certeza que deseja remover este plano?\n\nEsta ação não pode ser desfeita.')">🗑️</button>
              </form>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
  <div class="d-flex justify-content-center mt-3">
    <%= paginate @planos %>
  </div>
</div>
