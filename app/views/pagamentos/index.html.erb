<div class="container mt-5">
  
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1>💰 Pagamentos</h1>
    <div class="mb-3">
      <a href="/pagamentos/new" class="btn btn-success">➕ Adicionar</a>
      <a href="/pagamentos/export_pdf?format=pdf" class="btn btn-warning">📄 Exportar PDF</a>
    </div>
  </div>

  <div class="table-responsive">
    <table class="table table-striped">
      <thead class="table-dark">
        <tr>
          <th>ID</th>
          <th>Aluno</th>
          <th>Plano</th>
          <th>Valor</th>
          <th>Status</th>
          <th>Vencimento</th>
          <th>Pagamento</th>
          <th>Forma Pgto</th>
          <th>Referente a</th>
          <th>Ações</th>
        </tr>
      </thead>
      <tbody>
        <% @pagamentos.each do |pag| %>
          <tr>
            <td><%= pag.id %></td>
            <td><%= pag.aluno&.nome %></td>
            <td><%= pag.plano&.nome_plano %></td>
            <td>R$ <%= number_with_precision(pag.valor, precision: 2) %></td>
            <td><span class="badge bg-info"><%= pag.status %></span></td>
            <td><%= pag.data_vencimento&.strftime('%d/%m/%Y') || '—' %></td>
            <td><%= pag.data_pagamento&.strftime('%d/%m/%Y') || '—' %></td>
            <td><%= pag.forma_pagamento || '—' %></td>
            <td><%= pag.referente_a || '—' %></td>
            <td>
              <a href="/pagamentos/<%= pag.id %>/edit" class="btn btn-sm btn-warning">✏️</a>
              <form action="/pagamentos/<%= pag.id %>" method="POST" style="display:inline;">
                <input type="hidden" name="_method" value="DELETE">
                <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
                <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Tem certeza que deseja remover este pagamento?\n\nEsta ação não pode ser desfeita.')">🗑️</button>
              </form>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
  <div class="d-flex justify-content-center mt-3">
    <%= paginate @pagamentos %>
  </div>
</div>
