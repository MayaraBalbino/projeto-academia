<div class="container mt-5">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1>👥 Alunos</h1>
    <div class="mb-3">
      <a href="/alunos/new" class="btn btn-success">➕ Adicionar</a>
      <a href="/alunos/export_pdf?format=pdf" class="btn btn-warning">📄 Exportar PDF</a>
    </div>
  </div>
  
  <div class="table-responsive">
    <table class="table table-striped">
      <thead class="table-dark">
        <tr>
          <th>ID</th>
          <th>Nome</th>
          <th>CPF</th>
          <th>Telefone</th>
          <th>Status</th>
          <th>Plano</th>
          <th>Ações</th>
        </tr>
      </thead>
      <tbody>
        <% @alunos.each do |aluno| %>
          <tr>
            <td><%= aluno.id %></td>
            <td><%= aluno.nome %></td>
            <td><%= aluno.cpf %></td>
            <td><%= aluno.telefone %></td>
            <td><span class="badge bg-success"><%= aluno.status %></span></td>
            <td><%= aluno.plano&.nome_plano || '—' %></td>
            <td>
              <a href="/alunos/<%= aluno.id %>/edit" class="btn btn-sm btn-warning">✏️</a>
              <form action="/alunos/<%= aluno.id %>" method="POST" style="display:inline;">
                <input type="hidden" name="_method" value="DELETE">
                <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
                <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Tem certeza que deseja remover este aluno?\n\nEsta ação não pode ser desfeita.')">🗑️</button>
              </form>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
  <div class="d-flex justify-content-center mt-3">
    <%= paginate @alunos %>
  </div>
</div>
