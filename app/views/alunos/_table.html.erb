<div class="table-responsive">
  <table class="table table-striped table-hover">
    <thead class="table-dark">
      <tr>
        <th>ID</th>
        <th>Nome</th>
        <th>CPF</th>
        <th>Telefone</th>
        <th>Status</th>
        <th>Ações</th>
      </tr>
    </thead>
    <tbody>
      <% alunos.each do |aluno| %>
        <tr>
          <td><%= aluno.id %></td>
          <td><%= aluno.nome %></td>
          <td><%= aluno.cpf %></td>
          <td><%= aluno.telefone %></td>
          <td><span class="badge bg-success"><%= aluno.status %></span></td>
          <td>
            <%= link_to "✏️", edit_aluno_path(aluno), class: "btn btn-sm btn-warning" %>
            <%= link_to "🗑️", aluno_path(aluno), method: :delete, data: { confirm: "Tem certeza?" }, class: "btn btn-sm btn-danger" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
<div class="d-flex justify-content-center mt-3">
  <%= paginate alunos %>
</div>
