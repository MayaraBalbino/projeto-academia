<div class="table-responsive">
  <table class="table table-striped table-hover">
    <thead class="table-dark">
      <tr>
        <th>ID</th>
        <th>Aluno</th>
        <th>Professor</th>
        <th>Objetivo</th>
        <th>Ações</th>
      </tr>
    </thead>
    <tbody>
      <% treinos.each do |treino| %>
        <tr>
          <td><%= treino.id %></td>
          <td><%= treino.aluno&.nome %></td>
          <td><%= treino.professor&.nome %></td>
          <td><%= treino.objetivo %></td>
          <td>
            <%= link_to "✏️", edit_treino_path(treino), class: "btn btn-sm btn-warning" %>
            <%= link_to "🗑️", treino_path(treino), method: :delete, data: { confirm: "Tem certeza?" }, class: "btn btn-sm btn-danger" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
<%= paginate treinos %>
